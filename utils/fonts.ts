import { Fraunces, Nova_Mono } from '@next/font/google'

export const fraunces = Fraunces({
  subsets: ['latin'],
  variable: '--font-serif',
})
export const novaMono = Nova_Mono({
  subsets: ['latin'],
  variable: '--font-mono',
  weight: '400',
})
